
@{
    ViewData["Title"] = "Tables";
}

<h1>Create Tables: Result</h1>

<p>
    The ViewBag.containername container is created
    @(ViewBag.Success == true ? " Succeded! " : "failed because the table already exists!")
</p>
